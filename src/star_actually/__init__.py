"""The Loom — static build system for *, Actually.

Weaves a directory of depth-layered markdown nodes into a fully static,
navigable knowledge site. Domain-blind by design: everything it knows about
the subject matter arrives through ``nodes/`` and ``site.yaml``.
"""

__version__ = "0.3.0"
